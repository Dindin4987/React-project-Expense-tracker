import { configureStore } from '@reduxjs/toolkit';
import expenseReducer from '../redux/expensesSlice'

export const store = configureStore({
    reducer: {
        expenses: expenseReducer,
    },
});